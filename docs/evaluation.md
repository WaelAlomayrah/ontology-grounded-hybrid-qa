# Evaluation

Fifteen fixed questions run in vector-only, graph-only, and hybrid modes. Deterministic metrics avoid an ungrounded judge model. Confidence is a transparent evidence-volume/diversity heuristic rather than a probability. JSON results support reproducible thesis tables; CSV export can be produced from question rows with pandas.

